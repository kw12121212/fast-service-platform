# Tasks: rename-devops-to-engineering

## Implementation

- [x] Add delta specs that rename the ambiguous component area to engineering-oriented terminology.
- [x] Update RTK and current platform wording to use the new engineering term consistently.
- [x] Rename the backend placeholder package path from `devops` to `engineering` for the current extension-space ports.

## Testing

- [x] Verify the proposal artifacts and delta specs with `spec-driven.js verify`.
- [x] Run `mvn -q test` in `backend/`.

## Verification

- [x] Confirm the repository no longer uses `devops` as the current name for this platform component area.
