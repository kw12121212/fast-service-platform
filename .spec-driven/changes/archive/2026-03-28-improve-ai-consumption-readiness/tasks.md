# Tasks: improve-ai-consumption-readiness

## Implementation

- [x] Add an AI-first repository quickstart that centralizes required reading order, repository boundaries, common work locations, and verification entrypoints.
- [x] Add a machine-readable AI context manifest that captures stack baselines, directory responsibilities, required references, validation commands, and repository constraints.
- [x] Add high-frequency change playbooks for common backend, frontend, and integration-oriented extension paths.
- [x] Add repository-owned automated verification commands for backend checks, frontend checks, and full-stack smoke validation.
- [x] Add troubleshooting guidance for common local environment, dependency-installation, and backend-frontend integration failures.
- [x] Update any existing root or agent-facing documents needed so the new readiness assets become the obvious starting path.

## Testing

- [x] Verify the proposal artifacts and delta specs with `spec-driven.js verify`.
- [x] Run the automated backend verification entrypoint and confirm it covers the required backend baseline checks.
- [x] Run the automated frontend verification entrypoint and confirm it covers the required frontend baseline checks.
- [x] Run the automated full-stack smoke entrypoint and confirm it validates the current `/service/*` integration path.

## Verification

- [x] Confirm an AI contributor can identify what to read, where to edit, how to validate common changes, and how to troubleshoot common failures without reconstructing repository rules from scattered documents.
