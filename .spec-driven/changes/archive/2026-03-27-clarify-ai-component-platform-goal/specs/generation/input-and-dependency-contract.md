# Input And Dependency Contract Delta

## REMOVED Requirements

### Requirement: Natural Language As Primary Human Input
Removed because the active platform definition no longer treats in-repository input intake as a primary product concern.

### Requirement: Prototype Images As Supplemental Input
Removed because prototype-image handling belongs to an external AI workflow context rather than the repository's runtime scope.

### Requirement: Reference Websites Are Limited To UI Guidance
Removed because reference-website handling belongs to an external AI workflow context rather than the repository's runtime scope.

### Requirement: No Additional External Software Libraries Required
Removed from this spec because the active dependency contract now lives under the component-platform context.
